Pull from repo

Make directory with your name
It should have files named like:
    yourname_sprocs.sql (should contain sprocs you implemented)
Push the directory only, don't push the files you pulled from here if you changed them

Goodnight (7AM btw)